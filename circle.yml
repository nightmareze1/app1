machine:
  services:
    - docker
  python:
    version: 2.7
deployment:
  dockerhub:
    branch: master
     commands:
      - docker login -e "e_ariel@hotmail.es" -u "nightmareze1" -p "Eze123456!"
      - docker build -t "nightmareze1/alpine:$CIRCLE_BUILD_NUM" .
      - docker push "nightmareze1/alpine:$CIRCLE_BUILD_NUM"

test:
  override:   
    - mocha 
